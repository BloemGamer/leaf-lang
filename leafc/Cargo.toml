[package]
name = "leafc"
version = "0.1.0"
edition = "2024"
repository = "https://github.com/BloemGamer/leaf-lang/tree/main/leafc"

[features]
diag_source = []

[dependencies]
clap = { version = "4.5.59", features = ["derive"] }
leaf_proc = { path = "../leaf_proc" }
