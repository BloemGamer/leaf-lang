pub module prelude;
pub module alloc;
pub module io;

export @use core::*;
export @use std::prelude::*;
export @use core::prelude::*;

pub variant Result<T, E> {
	Ok(T),
	Err(E),
}







pub trait Copy{
}
